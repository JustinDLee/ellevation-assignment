/**
* Represents an entry in the salary history table
*/
function SalaryHistory(employeeId, salary, effectiveDate) {
    this.employeeId = employeeId;
    this.salary = salary;
    this.effectiveDate = effectiveDate;
}